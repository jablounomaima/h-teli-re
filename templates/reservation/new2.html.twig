
{% extends 'base.html.twig' %}

{% block title %}ajout des reservation{% endblock %}



{% block body %}
    <h1>Créer une nouvelle réservation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('list_reservation2') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}
